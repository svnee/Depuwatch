<%= form_for(@text_topic) do |f| %>
  <% if @text_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@text_topic.errors.count, "error") %> prohibited this text_topic from being saved:</h2>

      <ul>
      <% @text_topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text_id %><br />
    <%= f.text_field :text_id %>
  </div>
  <div class="field">
    <%= f.label :topic_id %><br />
    <%= f.text_field :topic_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
