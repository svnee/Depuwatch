<h1>Listing text_topics</h1>

<table>
  <tr>
    <th>Text</th>
    <th>Topic</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @text_topics.each do |text_topic| %>
  <tr>
    <td><%= text_topic.text_id %></td>
    <td><%= text_topic.topic_id %></td>
    <td><%= link_to 'Show', text_topic %></td>
    <td><%= link_to 'Edit', edit_text_topic_path(text_topic) %></td>
    <td><%= link_to 'Destroy', text_topic, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Text topic', new_text_topic_path %>
