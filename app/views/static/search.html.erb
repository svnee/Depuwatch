<div class="content_wrapper">
<div class="info"><a href="/search">Search</a>: weider Sichfunktiounen</div>

<div class="content_static">

	 <h1><img src="/images/113.gif"> Search</h1>
	<p> 
<img src="/images/file.gif"> Sich <strong>Dossier</strong> wou Deputéiert 
<%= select :deputy, :id, Deputy.all.collect {|d| [d.name, d.id]}, { :include_blank => true })%>
 vun der Partei 
<%= select :party, :id, Party.all.collect {|d| [d.name, d.id]}, { :include_blank => true })%> 
mat <input type="checkbox" name="vote" value="Jo"> Jo <input type="checkbox" name="vote" value="Nee"> Nee <input type="checkbox" name="vote" value="Abstentioun">
Abstentioun gestëmmt huet fir Sessioun
<%= select :session, :id, Session.all.collect {|d| ["#{d.start.strftime("%Y")}/#{d.end.strftime("%Y")}", d.id]}, { :include_blank => true })%>
an Thema 
<%= select :topic, :id, Topic.all.collect {|d| [d.name, d.id]}, { :include_blank => true })%>
. <input type="button" name="Search_text" value="Search Dossier">
</p>

<p> 
<img src="/images/136.gif"> Sich <input type="checkbox" name="vote" value="Jo"> aktuellen <input type="checkbox" name="vote" value="Nee"> freiren <strong>Deputéiert</strong> aus Bezierk 
<select name="bezierk" size="1" multiple="multiple">
      <option>All</option>
      <option>Norden</option>
      <option>Osten</option>
</select> vun der Partei 
<%= select :party, :id, Party.all.collect {|d| [d.name, d.id]}, { :include_blank => true })%> 
an Thema 
 <%= select :topic, :id, Topic.all.collect {|d| [d.name, d.id]}, { :include_blank => true })%>
mat Präsenz bei Voten iwwer <select name="bezierk" size="1">
      <option>0%</option>
      <option>10%</option>
      <option>20%</option>
    </select> an Vote/Delegatioun iwwer <select name="bezierk" size="1">
      <option>0%</option>
      <option>10%</option>
      <option>20%</option>
    </select>. <input type="button" name="Search_text" value="Search Deputy"">
</p>

</div>
