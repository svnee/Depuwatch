<%= form_for(@membership) do |f| %>
  <% if @membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@membership.errors.count, "error") %> prohibited this membership from being saved:</h2>

      <ul>
      <% @membership.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :deputy_id %><br />
    <%= f.text_field :deputy_id %>
  </div>
  <div class="field">
    <%= f.label :start %><br />
    <%= f.date_select :start, {:start_year => 2004} %>
  </div>
  <div class="field">
    <%= f.label :end %><br />
    <%= f.date_select :end, {:start_year => 2004} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
